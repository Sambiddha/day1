# git init
# git add .  or (git add --all)
# git commit -m "commit_message>"
# git branch -M <branch_name>
# git remote add origin <ssh link>
# git push -u origin <branch_name>

# git status #Shows the current status of all git staged and unstaged files


# Setting git configuration
# 1. git config --global user.name "<your-username>"
# 2. git config --global user.email "<your-email>"



#Generating ssh key pair
# ssh-keygen -t rsa -b 4096 -C "your email "

###git branching

# git branch # It shows all the branches and highlights the current branch

#git branch <branch_name>  #creates a new branch
# git checkout <branch_name> #jumps (checkout) to the next branch

# git checkout -b <branch_name> # this creates a new branch and jumps to that branch


